# Visual Execution Starter

```ts
const message = "Hello World";

const loudly = function (text: string): string {
  return text + "!";
};

const loudMessage = loudly(message);
console.log(loudMessage);
```

<iframe src="resources/visual-execution-1.pdf" height="500px">Visual Execution 1</iframe>

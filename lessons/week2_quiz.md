## Week 2 Quiz

* Explain the difference between "==" and "==="?

* What would be the result of 3+2+"7"?

* What are escape characters? Give some examples.

* Can you do everything that you can do in JavaScript also in TypeScript? What about the other way around?

* Is JavaScript case sensitive? Give an example

* What are these boolean operators called: ||, &&, ! 

* Name as many types that you can use in JavaScript.

* What's the difference between an object and an array?

* Are these values truthy or falsy (evaluate to true or false)

```js
true
42
0
"pizza"
"0"
"null"
false 
null
"undefined"
{}
undefined
[]
"" (an empty string)
NaN [](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/NaN)
```

What value of [BLANK] would make the following experrion evaluate to false

```js
!([BLANK] === 4) && "STRing" === "STRing"
```

Select the operator that would make the following expression evaluate to true.

```js
3 < -10 [BLANK] "James" !== "james"
```
Evaluate the following logical expressions. Do they return true or false?

```js
true || false 
false && false
!true
(13 > -7) || (false == 0)
(10 === "10") && (1 <= 2)
(3 != 6 % 3) && (24 > 25) && (!false)
```